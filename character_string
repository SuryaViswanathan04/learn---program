#include <stdio.h>
#include <string.h>
int main()
{
    int i;
   char word[50];
   int len =0;
   printf("Enter the string: ");
   scanf("%s",word);
   for(i=0;word[i]!='\0';i++)
   {
       len ++;
   }
    for(i=0;i<len;i++)
    {
        
        printf("%c ",word[i]);
    }
}
