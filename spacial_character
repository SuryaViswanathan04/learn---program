#include<stdio.h>
int main()
{
    char str[100];
    char ch='*';
    fgets(str,sizeof str,stdin);
    int i;
    for(i=0;str[i]!='\0';i++)
    {
        if(str[i]==' ')
        {
            str[i]=ch;
            
        }
    }
    printf("String is %s",str);
}
